{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "dev": {
      "cache": false
    },
    "@booking/types#build": {
      "inputs": ["src/**/*.ts"],
      "outputs": ["lib/**"],
      "outputMode": "new-only"
    },
    "@booking/configs#build": {
      "inputs": ["src/**/*.ts"],
      "outputs": ["lib/**"],
      "outputMode": "new-only"
    }
  }
}
